import 'package:flutter/material.dart';

class MyPurchasesMade extends StatelessWidget {
  const MyPurchasesMade({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Minhas Compras Realizadas'),
    );
  }
}
