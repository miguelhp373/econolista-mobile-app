import 'package:flutter/material.dart';

class MyShoppingLists extends StatelessWidget {
  const MyShoppingLists({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Minhas Listas de Compras'),
    );
  }
}
